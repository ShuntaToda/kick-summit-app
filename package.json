{
  "name": "kick-summit-app",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down",
    "db:seed": "tsx scripts/seed-local.ts --local",
    "db:seed:remote": "tsx scripts/seed-local.ts",
    "db:admin": "DYNAMO_ENDPOINT=http://localhost:8000 AWS_REGION=ap-northeast-1 AWS_ACCESS_KEY_ID=dummy AWS_SECRET_ACCESS_KEY=dummy dynamodb-admin",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "dynamodb-admin": "^5.1.3",
    "tsx": "^4.21.0",
    "turbo": "^2.8",
    "typescript": "^5.7"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "unrs-resolver"
    ]
  }
}
